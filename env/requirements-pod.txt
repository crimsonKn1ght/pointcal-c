# Declared pins for the paid Runpod run (CUDA 12.1 base image).
# These are the intended versions; the authoritative, reproducible record is
# env/requirements.lock.txt, produced by `pip freeze` on the pod after install
# (see scripts/runpod_setup.sh). If any pin below fails to resolve on the pod,
# record the substitution in docs/provenance.md instead of silently drifting.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.4.1+cu121
torchvision==0.19.1+cu121
open_clip_torch==2.24.0
huggingface_hub==0.23.4
numpy==1.26.4
pyyaml==6.0.1
tqdm==4.66.4
psutil==5.9.8
pandas==2.2.2
matplotlib==3.9.0
pytest==8.2.2
